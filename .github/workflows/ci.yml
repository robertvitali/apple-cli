name: CI

on:
  push:
    branches: [main]         # repo-local main-only workflow
  pull_request:
    # `edited` included so pr-title-lint re-runs when a PR title is fixed —
    # the title becomes the squash commit header (AGENTS.md branch model).
    types: [opened, edited, synchronize, reopened]

# Least privilege: the build/test job needs only read access to repo contents.
permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-test:
    # GitHub-hosted macOS runners ship full Xcode, so swift-testing is available here.
    # Hosted-runner minutes are free only while this repo is public; metered whenever it is private. Uses
    # pull_request, NOT pull_request_target — forked PRs get a read-only token and no secrets.
    # While public, fork PRs execute untrusted code (Package.swift manifests, test bodies) on these
    # runners; the read-only token bounds that to compute abuse. Keep "require approval for
    # outside-contributor runs" enabled in repo Actions settings.
    runs-on: macos-15
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4   # TODO(hardening): pin to a commit SHA + add Dependabot

      - name: Swift version
        run: swift --version

      - name: Build
        run: swift build -v

      - name: Unit tests (logic tier — swift-testing, no TCC / Apple permissions required)
        run: swift test -v

      # NOTE: the bats CLI-smoke tier does NOT run on hosted CI (measured 2026-08-30, run
      # 33288975170): observed failures were Mail tests needing a real Envelope Index (e.g.
      # mail search --sort exit codes), at ~10s/test (cancelled at 205/445, ~41 min); the
      # untested remainder (chat.db, AppleScript surfaces) is expected to fail the same way.
      # bats remains part of the LOCAL canonical pre-push suite (AGENTS.md "Toolchain +
      # testing") on a real Mac. The LIVE end-to-end tier likewise never runs in CI — it
      # needs granted TCC — and must NEVER be wired to a pull_request trigger from a fork.

  # Squash-merge makes the PR TITLE the commit header that lands on main (AGENTS.md
  # branch model), so the title must be Conventional BEFORE merge — the post-merge
  # commit-lint run on main can only redden trunk after the fact. Runs on the
  # title-bearing PR events; no checkout needed (the title is event payload).
  pr-title-lint:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Validate PR title as the future squash commit header
        env:
          TITLE: ${{ github.event.pull_request.title }}
        run: |
          set -euo pipefail
          printf '%s\n' "$TITLE" | grep -qE '^(feat|fix|docs|style|refactor|perf|test|build|ci|chore|revert)(\([a-z0-9._-]+\))?!?: .{1,}$' \
            || { printf 'PR title becomes the squash commit header on main; it must be a Conventional Commit header. Got: %s\n' "$TITLE"; exit 1; }

  # The release workflow (release.yml) computes bump levels from Conventional Commit
  # headers, so malformed headers silently corrupt version math. This job hard-fails
  # any pushed commit whose subject is not Conventional (git-mechanical forms exempt).
  commit-lint:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Validate Conventional Commit headers
        env:
          EVENT_NAME: ${{ github.event_name }}
          BASE_REF: ${{ github.base_ref }}
          BEFORE_SHA: ${{ github.event.before }}
        run: |
          set -euo pipefail
          SUBJECTS="$RUNNER_TEMP/subjects.txt"
          if [ "$EVENT_NAME" = "pull_request" ]; then
            git fetch origin "$BASE_REF" --quiet
            git log --format=%s "origin/${BASE_REF}..HEAD" > "$SUBJECTS"
          elif [ -n "$BEFORE_SHA" ] && ! echo "$BEFORE_SHA" | grep -qE '^0+$' \
               && git rev-parse --verify --quiet "${BEFORE_SHA}^{commit}" >/dev/null; then
            git log --format=%s "${BEFORE_SHA}..HEAD" > "$SUBJECTS"
          else
            # force-push / initial push: bounded recent window (never a range that can fail)
            git log --format=%s -n 20 HEAD > "$SUBJECTS"
          fi
          BAD=0
          while IFS= read -r subj; do
            [ -n "$subj" ] || continue
            if echo "$subj" | grep -qE '^(Merge |Revert "|fixup!|squash!|amend!|WIP)'; then continue; fi
            if ! echo "$subj" | grep -qE '^(feat|fix|docs|style|refactor|perf|test|build|ci|chore|revert)(\([a-z0-9._-]+\))?!?: .{1,}$'; then
              echo "NOT CONVENTIONAL: $subj"; BAD=1
            fi
          done < "$SUBJECTS"
          [ "$BAD" = "0" ] || { echo "Commit headers must follow Conventional Commits (release.yml depends on them)."; exit 1; }
